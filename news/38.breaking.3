Require ``setuptools >= 42.0.2``.  Earlier versions give ImportErrors on the Python versions we support.
Since this is higher than 38.2.3, we are sure to have support for wheels.
Remove support for ``distribute``, which was probably already broken.
[maurits]
