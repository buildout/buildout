language: python
env:
    - PYTHON_VER=2.6
    - PYTHON_VER=2.7
    - PYTHON_VER=3.2
    - PYTHON_VER=3.3

branches:
  only:
    - conditional_expressions

notifications:
  email:
    - pombredanne@gmail.com

install:
    - deactivate
    - make build

script: make test
